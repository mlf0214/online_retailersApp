<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.online_retailersapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        tools:replace="android:theme"
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Online_retailersApp">
        <activity android:name=".ui.home.HomeActivity"/>
        <activity android:name=".ui.user.RegisterActivity"/>
        <activity android:name=".ui.commodity.CommodityActivity"/>
        <activity android:name=".ui.order.OrderActivity"/>
        <activity android:name=".ui.user.SetPsdActivity"/>
        <activity android:name=".ui.user.SetUserAddressActivity"/>
        <activity android:name=".ui.user.SetUserDataActivity"/>
        <activity android:name=".ui.user.SelectUserOrdersActivity"/>
        <activity android:name=".ui.user.SetUserAddressDataActivity"/>
        <activity android:name=".ui.user.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
    </application>

</manifest>